require "spec_helper"

describe Tane::Commands::Login do

  describe ".process" do
    it "should save credentials if they are valid" do
      pending
    end

    it "should display a message if the credentials are not valid" do
      pending
    end
  end
end
