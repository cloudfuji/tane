require "spec_helper"

describe Tane::Commands::Exec do

  describe ".process" do
    it "should set the Bushido environment vars" do
      pending
    end

    it "should call exec to replace the current process with the arguments passed" do
      pending
    end
  end

end
